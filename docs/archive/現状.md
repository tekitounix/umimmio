以下、コードベース照合ベースでの分類です（削除は行いません）。

不要（現行コード/実装と不一致、または役割終了）

KERNEL_SCHEDULER_REDESIGN.md：wait_block()の修正は既に実装済みで、計画としての役割が完了（実装は umi_kernel.hh）。
WEB_UI_REDESIGN.md：内容は現行UIに反映済みで提案書としては役割終了（現行UIは index.html）。
MEMO.md：内容が断片的で NOMENCLATURE.md と重複、参照先も見当たらず。
PLAN_APP_MEMORY_AND_FAULT.md：実装未反映（app.ldは固定4KBスタックのまま、SharedMemoryにMemoryUsageなし）。現行と乖離（app.ld、loader.hh）。
UMIOS_STORAGE.md：storage実装が存在せず、現行コードで未使用（umios）。
UMI_STATUS_PROTOCOL_DESIGN.md：設計内容（Idle処理等）が現行実装と一致しない（現行はSystemTask側でSysEx処理、kernel.cc）。
docs/archive/UXMP_SPECIFICATION.md、docs/archive/UXMP_DATA_SPECIFICATION.md、docs/archive/UXMP提案書.md：コード側の実装/参照が見当たらず、現行では未使用。
不要ではない（現行と整合/参照あり）

DESIGN_CONTEXT_API.md：AudioContextとProcessor/Controller分離が現行コードで採用（main.cc）。
PLAN_AUDIOCONTEXT_REFACTOR.md：上記と同じく、現行の処理モデルに一致。
docs/archive/UMI_SYSEX_PROTOCOL.md：Web側の実装が一致（protocol.js）。
UMIOS_DESIGN_DECISIONS.md：syscall番号体系などが現行ヘッダと一致（syscall.hh）。
STM32F4_KERNEL_FLOW.md：タスク構成・処理フローが現行実装と整合（kernel.cc）。
UMIOS_CONTENTS.md：umiosの構成説明として現行ツリーと整合。
UMI_SYSTEM_ARCHITECTURE.md：主要設計資料として参照されており有用（CLAUDE.md）。
NOMENCLATURE.md：用語/命名規約の基準として有用。
OPTIMIZATION_PLAN.md：LTOなど現行ビルド設定と対応する検討ログとして有用（xmake.lua）。