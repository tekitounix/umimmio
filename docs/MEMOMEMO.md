00

Server Taskの優先度はUser Taskと同じにするべきか、それともUser Taskと同列のタスクがもう一つ必要か

Controller はターゲットほぼ非依存のところ、AppはConfigで要求を出す、OSはケイパビリティベースで要求に応える、Appは最低限のヘッドレス動作を実装していればヘッドレス部分+互換性のある機能があらゆるターゲットで動作する

ライセンス管理の仕組みが作れないか？
アプリケーションの認証の仕組みに入れ込む

超コンパクトで堅牢、常にバックアップされるミニマルなアカウント認証サーバーを作り、

組み込み環境のみを想定しているので動的なポート変更は必要ないです
USBデバイスや拡張ボードと言ったものの接続によって入出力が変化するとしても、メモリ資源は有限なので静的確保した範囲内で動作するように設計します
その上で拡張デバイスが接続されたらspanで渡すポート数を変化させる必要がある